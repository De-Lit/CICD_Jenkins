#!groovy
// Check ub1 properties
properties([disableConcurrentBuilds()])

pipeline {
    stages {
        stage("First step") {
            steps {
                echo "First step"
            }
        }
        stage("Second step") {
            steps {
                echo "Second step"
            }
        }
    }
}
